---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: talosconfigtemplates.bootstrap.cluster.x-k8s.io
spec:
  group: bootstrap.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: TalosConfigTemplate
    listKind: TalosConfigTemplateList
    plural: talosconfigtemplates
    singular: talosconfigtemplate
  scope: Namespaced
  versions:
  - name: v1alpha2
    schema:
      openAPIV3Schema:
        description: TalosConfigTemplate is the Schema for the talosconfigtemplates
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TalosConfigTemplateSpec defines the desired state of TalosConfigTemplate
            properties:
              template:
                description: TalosConfigTemplateResource defines the Template structure
                properties:
                  spec:
                    description: TalosConfigSpec defines the desired state of TalosConfig
                    properties:
                      data:
                        type: string
                      generateType:
                        type: string
                    required:
                    - generateType
                    type: object
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: false
  - name: v1alpha3
    schema:
      openAPIV3Schema:
        description: TalosConfigTemplate is the Schema for the talosconfigtemplates
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TalosConfigTemplateSpec defines the desired state of TalosConfigTemplate
            properties:
              template:
                description: TalosConfigTemplateResource defines the Template structure
                properties:
                  spec:
                    description: TalosConfigSpec defines the desired state of TalosConfig
                    properties:
                      configPatches:
                        items:
                          properties:
                            op:
                              type: string
                            path:
                              type: string
                            value:
                              x-kubernetes-preserve-unknown-fields: true
                          required:
                          - op
                          - path
                          type: object
                        type: array
                      data:
                        type: string
                      generateType:
                        type: string
                      hostname:
                        description: Set hostname in the machine configuration to
                          some value.
                        properties:
                          source:
                            description: |-
                              Source of the hostname.

                              Allowed values:
                              "MachineName" (use linked Machine's Name).
                              "InfrastructureName" (use linked Machine's infrastructure's name).
                            type: string
                        type: object
                      strategicPatches:
                        description: Talos Linux machine configuration strategic merge
                          patch list.
                        items:
                          type: string
                        type: array
                      talosVersion:
                        type: string
                      variables:
                        description: |-
                          List of variables to expose to the config template.
                          Variables can be used in StrategicPatches and data fields.
                          GoTemplate must be enabled for this to work.
                        items:
                          description: Variable is a definition of a variable to be
                            exposed to the config template.
                          properties:
                            name:
                              description: Name of the variable to be exposed to the
                                template.
                              type: string
                            value:
                              description: Value of the variable to be exposed to
                                the template.
                              type: string
                            valueFrom:
                              description: Specifies a source the value of this var
                                should come from.
                              properties:
                                secretKeyRef:
                                  description: SecretKeySelector selects a key of
                                    a Secret.
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                    required:
                    - generateType
                    type: object
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
